---
import SanityImage from './SanityImage.astro';

const { asset, alt, name } = Astro.props.node

/* console.log(Astro.props.node) */
---

<SanityImage
  asset={asset}
  alt={alt}
  containerDim={{
    width: 200,
    height: 200
  }}
/>